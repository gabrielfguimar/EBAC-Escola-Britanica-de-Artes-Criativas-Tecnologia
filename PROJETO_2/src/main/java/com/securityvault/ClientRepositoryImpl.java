package com.securityvault; // Adicione isso no topo do seu Main.java

public class ClientRepositoryImpl extends AbstractVaultRepository<ClientEntity> {
    // Como ele estende AbstractVaultRepository, ele já herda os métodos save(), findById(), etc.
    // Não precisa de código adicional por enquanto!
}